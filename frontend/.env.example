# Backend URL (set this in Railway for the frontend service)
VITE_API_URL=https://your-backend.railway.app
# Optional: override WS URL separately (derived from VITE_API_URL if not set)
# VITE_WS_URL=wss://your-backend.railway.app/ws
